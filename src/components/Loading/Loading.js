// == Import
import './loading.css'
import propTypesLib from 'prop-types';

// == Composant
function Loading() {
    return (
        <span className="loader inherit"></span>
    );
}

// == Export
export default Loading;
